#Merge Sort
#Best Case: O(n log(n))
#Average Case: O(n log(n))
#Worst Case: O(n log(n))

def quickSort(aList):
    '''Quick sort
       Takes an list of comparable values and sorts them using
       quick sort before returning them in a list'''
    pass()

if __name__ == "__main__":
    a = [4, 1, 8, 10, 3, 6]
    print("Unsorted:", a)
    a = quickSort(a)
    print("Sorted:", a)
